p There are {{projects.length}} projects
section(ng-repeat='item in countSections()').bg-grey
    div.container
        div.row.equal
            div(ng-repeat='post in projects|myLimitTo:3:limitToStartIndex($index)').col-sm-4
                div.project-col
                    img(src='{{post.image}}').full-width
                    div.project-col-info
                        h3.project-col-header
                            | {{post.title}}
                        p {{post.text}}

// EXAMPLE Start
//div.container
//    div.row
//        div.col-md-12
//div(ng-repeat='post in posts').col-md-6
//        img(src='{{post.image}}')
//        h3 {{post.title}}
//        div {{post.text}}
//        //a(href='readPost/{{post.id}}') More
//        //|  -
//        //a(href='editPost/{{post.id}}') Edit
//        //|  -
//        //a(href='deletePost/{{post.id}}') Delete
// EXAMPLE End